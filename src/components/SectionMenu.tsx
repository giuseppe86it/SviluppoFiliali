import { sections } from '../sections'
export function SectionMenu({inspectionId,current,onClose}:{inspectionId:string;current:string;onClose:()=>void}){
  return <div className="modal-backdrop" onClick={onClose}><div className="section-drawer" onClick={e=>e.stopPropagation()}><div className="card-heading"><h3>Sezioni sopralluogo</h3><button className="icon-btn" onClick={onClose}>×</button></div><button className="section-link rapid-link" onClick={()=>{location.hash=`#/rapid/${inspectionId}`;onClose()}}><span>⚡</span>Sopralluogo rapido</button>{sections.map((s,i)=><button key={s.id} className={`section-link ${s.id===current?'active':''}`} onClick={()=>{location.hash=`#/inspection/${inspectionId}/${s.id}`;onClose()}}><span>{String(i+1).padStart(2,'0')}</span>{s.title}</button>)}</div></div>
}
